/// ⏱️ المدد الزمنية المستخدمة (animations – delays – إلخ)
class AppDurations {
  static const Duration short = Duration(milliseconds: 300);
  static const Duration medium = Duration(seconds: 4);
  static const Duration long = Duration(seconds: 1);
}
